元素-表达式：lambda argument_list：expression
lambda x, y: x*y     函数输入是x和y，输出是它们的积x*y
add=lambda x, y: x+y
add(1,2)=3
将lambda函数作为参数传递给其他函数
找出3的倍数：
list(filter(lambda x: x % 3 == 0, [1, 2, 3]))
map函数：
list(map(lambda x: x+1, [1, 2,3]))

i for i in xx if xx 表达式： 带有i的expression for i in 某范围 if 带i的expression。
可以看成：
 for i in xx：
    if xx：
        doing

例如：
>>> [i+1 for i in range(5) if i>1]
[3, 4, 5]
